version: "3.9"

services:
  react-vite-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: react-vite-app
    ports:
      - "8080:80"
    restart: unless-stopped
